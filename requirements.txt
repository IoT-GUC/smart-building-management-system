fastapi==0.128.0
uvicorn[standard]==0.40.0
requests==2.32.3
truststore==0.10.4
python-dotenv==1.2.1
pydantic==2.12.5
pydantic-settings==2.12.0
Pillow==11.0.0
python-multipart==0.0.21
websockets==16.0
httpx==0.28.1
Jinja2==3.1.4
anyio[trio]==4.7.0
# AES-CMAC for LoRaWAN join-request MIC verification (app/services/lorawan_join.py).
# Previously present only as a transitive dependency; pinned now that it is imported directly.
cryptography==42.0.3
